# --- web framework ---
fastapi>=0.115
uvicorn[standard]>=0.30

# --- crawler ---
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.2
aiohttp>=3.10

# --- config / models ---
pydantic>=2.7
pydantic-settings>=2.3

# --- AI ---
anthropic>=0.49

# --- reports ---
jinja2>=3.1
weasyprint>=62      # PDF export; needs native libs (pango/cairo). Optional — HTML works without it.
python-docx>=1.1    # Word (.docx) export

# --- persistence (Phase 5, optional) ---
sqlalchemy>=2.0
psycopg[binary]>=3.1

# --- dev / test ---
pytest>=8.2
pytest-asyncio>=0.23
